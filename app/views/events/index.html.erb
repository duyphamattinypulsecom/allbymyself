<div class="container-fluid events-list">
    <% @events.in_groups_of(2) do |g| %>
      <div class="row">
        <% g.compact.each do |e| %>
            <%= render partial: 'card', locals: {event: e, editable: true} %>
        <% end %>
      </div>  
    <% end %>
  </div>
</div>